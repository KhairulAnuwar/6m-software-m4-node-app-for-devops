version: 2.1

orbs:
  node: circleci/node@5.0.1
  docker: circleci/docker@2.1


jobs:
  build:
    docker:
      - image: cimg/node:16.10
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: npm
      - run: |
          echo "Install dependencies"
          npm install
  test:
    docker: 
      - image: cimg/node:16.10
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: npm
      - run: |
          echo "Running tests"
          npm run test
  build-and-push:
    docker:
      - image: cimg/node:16.10
    steps:
      - setup_remote_docker
      - checkout
      - docker/login:
          username: $DOCKER_HUB_USERNAME
          password: $DOCKER_HUB_PASSWORD
      - run: |
          echo "Building Docker Image"
          docker build -t education-space/your-app:latest .
      - run: |
          echo "Pushing Docker Image"
          docker push education-space/your-app:latest


workflows:
  simple_workflow:
    jobs:
      - build
      - test:
          requires: 
            - build
      - build-and-push:
          requires:
            - test
